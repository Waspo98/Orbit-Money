name: Deploy Live

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: deploy-live
  cancel-in-progress: true

permissions:
  contents: read
  packages: write

jobs:
  deploy:
    name: Publish and deploy live Docker container
    runs-on: [self-hosted, Windows, X64]
    steps:
      - name: Deploy live on home server
        shell: cmd
        env:
          GITHUB_ACTOR: ${{ github.actor }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ORBIT_PUBLISH_IMAGE: "1"
        run: call "C:\Docker\Orbit Money\scripts\deploy-live.cmd"
